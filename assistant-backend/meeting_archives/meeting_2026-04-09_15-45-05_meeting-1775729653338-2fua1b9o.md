# Meeting Archive: meeting-1775729653338-2fua1b9o
**Date:** April 09, 2026 at 03:45 PM

---

### user
tell me about Advanced RAG Pipeline project

### assistant
I built an enterprise AI data architecture prototype called an Advanced RAG Pipeline using Python, LangChain, and Google Gemini 1.5 Flash. To handle the data, I used FAISS for dense retrieval, BM25 for sparse keyword matching, and UnstructuredIO for complex document layout parsing. A primary challenge I overcame was LLM hallucinations and context noise, which I solved by implementing a two-stage retrieval process with a HuggingFace BGE-Reranker. I also addressed the gap between semantic and keyword search by architecting a hybrid ensemble retriever with a 70/30 weight distribution. This design ensured that the LLM only received the top three highest-fidelity chunks, operating at a temperature of 0.0 for deterministic generation. The final result was a system with near-zero hallucination rates that acts as a perfectly grounded oracle for mission-critical deployments.

### user
🎧 **[Live System Audio]:**  How would you design the back end architecture for our marketing OS 2? handle 10,000 concurrent AI agent executions and real-time data ingestion from social media You

### user
Quick Answer

### assistant
I'd use an event-driven architecture with a message broker like Kafka to decouple ingestion from execution. For 10k concurrent agents, I'd deploy a supervisor-worker pattern using LangGraph for state management and checkpointing. I'd scale the agent workers using a serverless compute layer to handle the bursty load. Real-time social data would flow through a streaming pipeline into a vector database for immediate retrieval. I'd implement strict Pydantic schema validation for all tool calls to prevent agent crashes. To manage costs, I'd use a fast model for routing and a larger one only for final reasoning. I'd also add human-in-the-loop checkpoints for high-stakes marketing actions.

### user
🎧 **[Live System Audio]:** purpose.

