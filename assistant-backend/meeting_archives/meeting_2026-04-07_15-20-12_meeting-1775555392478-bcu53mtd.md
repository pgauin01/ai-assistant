# Meeting Archive: meeting-1775555392478-bcu53mtd
**Date:** April 07, 2026 at 03:20 PM

---

### user
hi

### assistant
Out of scope. I only assist with software engineering.

### user
tell me about kirana store

### assistant
I built the 1K Kirana Store, a B2B retail tech platform that connects small Kirana shop owners with e‑commerce tools. I led the full‑stack migration from a legacy PHP monolith to a modern React front‑end and Node.js back‑end, exposing GraphQL APIs on AWS EC2. The biggest technical hurdle was ensuring transactional consistency across pricing, inventory, and order workflows under high concurrency, so I architected modular rule engines and isolated database transactions. I also established strict component standards and code‑review practices to lift the front‑end reliability by about twenty percent and accelerate feature delivery. The result was a production‑ready platform that handles roughly fifty thousand API requests per day, serves four to five hundred daily active users, and supports around five thousand monthly active users with zero downtime. Overall, the project delivered a scalable, reliable e‑commerce solution that empowers Kirana shop owners to manage their inventory and sales efficiently.

### user
tell me about Advanced RAG Pipeline project

### assistant
I built an advanced RAG pipeline that stitches together Python, LangChain, Google Gemini 1.5 Flash, FAISS, BM25, HuggingFace models, and Selenium for scraping. The core challenge was scraping modern JavaScript‑heavy blogs that block bots, so I engineered a SeleniumURLLoader with headless Chrome and memory tweaks to reliably pull the DOM text. I tackled the semantic versus keyword retrieval gap by creating a hybrid ensemble that pulls from FAISS and BM25, weighting them 70/30, and then reranking the top ten with a BGE cross‑encoder to prune noise. This two‑stage retrieval keeps the context tight and eliminates hallucinations, letting Gemini produce factually grounded answers at temperature zero. The final result is a production‑ready prototype that can ingest any web page, retrieve precise and conceptual answers, and maintain near‑zero hallucination rates. I showcased it as a portfolio piece, proving that hybrid retrieval and rigorous context compression can solve enterprise AI data architecture challenges.

### user
🎧 **[Live System Audio]:**  That architecture makes sense, but Why did you choose to use local FAISS in S&P? instead of a managed cloud vector database like Pineco. or we wait.

### user
Career & Project Follow-up

### assistant


### assistant
I started with local FAISS because it was fast to prototype and gave me full control over the index format. But once we moved to production, keeping an in‑memory index inside a stateless FastAPI container made scaling a nightmare – we had to sync state across replicas. Switching to Pinecone let us keep the API stateless and offload persistence, sharding, and scaling to the service, which is essential for a cloud‑deployed application.

### user
🎧 **[Live System Audio]:**  You mentioned using cross encoder for rear ranking. Those are com- computationally massive, how did you prevent that from and destroying your OP response times.

