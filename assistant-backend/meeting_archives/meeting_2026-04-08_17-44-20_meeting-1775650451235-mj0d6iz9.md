# Meeting Archive: meeting-1775650451235-mj0d6iz9
**Date:** April 08, 2026 at 05:44 PM

---

### user
🎧 **[Live System Audio]:**  Can you explain the aka- architecture behind code generating to like cursor. Like how does it actually know what I want to type next?

### user
🎧 **[Live System Audio]:**  Can you explain the architecture behind code? generating to like Gursa, like how does it actually know what... that I want to type next without hallucinating.

### user
Quick Answer

### assistant


### user
Quick Answer

### assistant
Cursor (or Gursa) uses a transformer‑based LLM that predicts the next token given the code context. The model was pre‑trained on massive public codebases, so it learns common APIs and idioms. During inference it scores all possible tokens and picks the highest‑probability one, optionally filtered by a safety layer. To reduce hallucinations it conditions on the exact surrounding syntax and can invoke a retrieval module for relevant snippets. The retrieval step queries a vector DB of indexed project files, injecting those results as additional context. A low temperature and top‑k sampling further keep outputs deterministic and grounded in the retrieved code. If the model’s confidence drops, the system falls back to a human‑in‑the‑loop suggestion or blocks the completion.

### user
🎧 **[Live System Audio]:**  which is MCP, Model Context Prote and why is it important?

### user
Quick Answer

### assistant
MCP stands for Model Context Protection, a pre‑inference step that sanitizes the prompt sent to the LLM. It strips out unrelated code, comments, or private tokens that could confuse the model. By feeding only the relevant slice, the model’s probability distribution stays focused, cutting hallucinations. It also keeps the request inside the model’s token window, saving latency and cost. For code‑completion tools like Cursor, MCP ensures suggestions respect the current file’s scope and security policies. So the protection layer is key to reliable, low‑noise completions in production.

### user
🎧 **[Live System Audio]:**  How do you structure the reason right pointing? Parenting Hero Act Right-Pointing Hero Observe Loop in production

### user
Quick Answer

### assistant
The Right‑Pointing Hero loop is an agent that spawns a Hero worker, runs its action, observes output, then repeats reasoning. We keep the loop fast by storing the shared State Dictionary after each observe step. Checkpointing the state lets us resume if the worker crashes or hits a recursion limit. A Max Recursion Depth guard stops infinite cycles, and Pydantic schemas validate every tool output. For risky actions we insert a Human‑In‑The‑Loop approval checkpoint before committing changes. The supervisor routes cheap routing queries to a small model and only escalates complex decisions to the large LLM.

### user
🎧 **[Live System Audio]:**  When would you use a smaller model vs a larger model? in agents.

