# Meeting Archive: meeting-1775719475493-ji32bbob
**Date:** April 09, 2026 at 12:55 PM

---

### user
tell me about Advanced RAG Pipeline project

### assistant
I built an Advanced RAG Pipeline that combines hybrid search with cross-encoder reranking to create a highly accurate question-answering system for complex academic documents. The tech stack includes Python, LangChain, Google Gemini 1.5 Flash, FAISS for dense retrieval, BM25 for sparse keyword matching, and HuggingFace models like all-MiniLM for embeddings and BGE-Reranker for reranking. The primary technical challenge I solved was eliminating LLM hallucinations and bridging the gap between semantic understanding and exact keyword matching, which standard vector search completely fails at. I engineered a two-stage retrieval system where FAISS and BM25 first fetch the top 10 candidates in a 70-30 weighted ensemble, then a computationally heavy Cross-Encoder reranks them and filters down to only the top 3 highest-quality chunks before feeding them to the LLM. I also tackled complex PDF parsing issues by using UnstructuredIO to preserve multi-column layouts and table structures that standard loaders would scramble. The result was near-zero hallucination rates and a system that handles both conceptual questions and exact entity queries with production-grade accuracy.

### user
🎧 **[Live System Audio]:**  Can you explain how code generating? do like us. Can you explain how code generating tool-like cursor works? Like how does it know what to type without hallucinating?

### user
Quick Answer

### assistant
Cursor and similar AI IDEs use Fill-In-The-Middle prompting where the model sees code before and after your cursor position to predict what should go in between. They gather deep context by parsing your entire codebase using AST analysis, reading all open tabs, and hooking into Language Server Protocols to understand type definitions and function signatures. This context window can be 100k+ tokens, so the model literally sees your project structure, imports, and variable names before generating a single character. To reduce hallucinations they validate suggestions against your actual type system in real-time and only show completions that compile. The key difference from ChatGPT is this isn't a chat interface, it's streaming token prediction with live syntax validation happening on every keystroke. They also use smaller specialized models like CodeLlama or StarCoder for speed, reserving GPT-4 class models only for complex refactoring requests. The result is autocomplete that feels psychic but is really just massive context plus aggressive filtering.

### user
🎧 **[Live System Audio]:**  which is MCP, model control. text protocol, and why is it important?

### user
Quick Answer

### assistant


